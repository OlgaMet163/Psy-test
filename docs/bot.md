# Документация Telegram-бота

## Цели бота
- Собрать ответы по тестам (HEXACO, SVS, Hogan DSUSI-SF) и выдать интерпретации с визуализациями.
- Поддержать базовые админские операции (локальный экспорт), без внешних интеграций.
- Мэтчинг и расширенные отчёты — в планах.

## Основные сценарии диалога
1. Приветствие (`/start`) + меню с тремя тестами (кнопки с эмодзи).
2. Запуск выбранного теста; клавиатура меню скрывается на время прохождения.
3. Ответы через инлайн-кнопки 1–5 (варианты зависят от теста/вопроса).
4. Завершение теста → текстовые выводы + PNG-радар (HEXACO, Hogan, SVS).
5. Повторный просмотр результатов по командам вида «📊 результаты <тест>».
6. `/reset` — очистка истории пользователя и возврат в стартовое меню.

## Состояния пользователя
- `idle`: тест не выбран.
- `HexacoStates.answering` / `HoganStates.answering` / `SvsStates.answering`: тест в процессе, индекс вопроса и накопленные ответы хранятся в FSM (MemoryStorage).
- Параллельный запуск другого теста блокируется; предложение завершить текущий или отправить `/reset`.

## Интеграции
- Telegram Bot API, режим long polling.
- Локальное хранилище SQLite (`data/bot.db`) через `StorageGateway`: таблицы `hexaco_answers` и `hexaco_results` переиспользуются всеми тестами.
- Выгрузка CSV для HEXACO: `python scripts/export_hexaco.py --output exports`.

## Админские функции
- Сейчас: выгрузка HEXACO в CSV через CLI-скрипт.
- В планах: расширенные отчёты, управление текстами и мэтчинг.

## UX-заметки
- Кнопки ответов однотипные по шкале 1–5, порядок фиксирован; для некоторых вопросов HEXACO вариант текста меняется (см. `bot/keyboards/common.py`).
- Клавиатура меню скрывается на время теста, возвращается после завершения.
- Команды с эмодзи обрабатываются в разных регистрах/языках (например, `🚀 начать hexaco`, `📊 hogan results`).

