# Видение продукта

## Цель
Дать пользователям удобный способ пройти ключевые психологические опросники в Telegram с моментальной расшифровкой и визуализацией. Мэтчинг по совместимости профилей остаётся в планах.

## Аудитория
- Люди, интересующиеся саморазвитием и ценностями.
- HR/коучи для экспресс-скрининга.
- Пользователи, ищущие совместимость по стилю поведения и ценностям.

## Что доступно сейчас
- 3 теста: HEXACO (24), SVS (20), Hogan DSUSI-SF (38).
- Результаты сразу в интерфейсе; все три теста дополняются PNG-радаром.
- Меню только inline, порядок кнопок: «Начать» → «Результаты» → «Перепройти» (по доступности результатов); параллельные тесты запрещены.
- Участник вводит почту; если по почте есть результаты — показываются кнопки «Результаты/Перепройти», иначе только «Начать».
- Сотрудник: команда `teamswitch` → кнопка «Посмотреть участника» → ввод почты участника (регистронезависимо) → IM + базовые Hogan + блок «Кураторам».
- `/reset` очищает личную историю (ответы, результаты, почта) и возвращает стартовое приветствие.
- Данные хранятся локально в SQLite (`data/bot.db`); экспорт HEXACO — `python scripts/export_hexaco.py --output exports`.

## Приветственное сообщение (фактический текст в боте)
```
Добро пожаловать! 🔮

Этот бот предлагает 3 психологических теста:
• HEXACO
• Ценностный опросник Шварца (SVS)
• Hogan DSUSI-SF

Все три суммарно занимают ~15–20 минут. Отвечайте, исходя из того, какое поведение характерно для вас в последние 2–3 месяца. После завершения тестов вы получите текстовые выводы о себе и графики по метрикам.

При прохождении теста можно делать паузы — ответы сохраняются, результаты всегда доступны в меню.
```
Отправляется как caption к GIF `bot/assets/welcome.gif`, при отсутствии файла уходит текстом.

## Пользовательский путь
1) `/start` → приветствие + inline-меню.  
2) Ввод почты → меню действий по доступным тестам/результатам для этой почты.  
3) Прохождение теста → PNG-радар + текстовые выводы.  
4) `/reset` — очистить историю и начать заново.

## Роли и возможности
### Пользователь
- Проходит тесты, видит результаты и интерпретации.
- Может ставить на паузу; запуск другого теста невозможен, пока идёт текущий.
- Результаты и доступные кнопки зависят от введённой почты; очистка — через `/reset`.

### Сотрудник
- Команда `teamswitch` → «Посмотреть участника» → ввод почты участника.
- Получает IM и базовые Hogan, а также полный блок «Кураторам».

### Админ (в планах/частично)
- Сейчас: экспорт HEXACO через CLI-скрипт.
- В будущем: расширенные отчёты, настройка интерпретаций, мэтчинг.

## Ограничения и этика
- Результаты — не клинический диагноз.
- Данные показываются только пользователю; шаринг — по явному согласию.
- Храним минимум PII (Telegram ID); при необходимости данные можно стереть (`/reset`).

