# Psy-test Bot

## О проекте
Телеграм-бот на aiogram 3 для прохождения психологических тестов с мгновенной выдачей результатов и визуализацией. Доступны:
- HEXACO two-facet (24 утверждения, шкала 1–5, проценты и интерпретации).
- Schwartz Value Survey (SVS, 20 утверждений, шкала 1–7, 10 ценностей + 4 группы).
- Hogan DSUSI-SF (38 утверждений, шкала 1–5, 11 шкал + Impression Management) + радар-график.

## Текущее состояние
- Реализованы три теста, ответы и результаты хранятся в локальной SQLite (`data/bot.db`).
- Меню с эмодзи: 📊 results / 🔁 restart / 🚀 start; запуск нового теста блокируется, если другой уже идёт.
- `/reset` удаляет историю пользователя (ответы и результаты) и возвращает к стартовому экрану.
- Hogan отдаёт PNG-радар при завершении теста и по запросу результатов.

## Установка и запуск
1. Python 3.11+, виртуальное окружение (рекомендуется).
2. Зависимости:
   ```bash
   pip install -r requirements.txt
   ```
3. Скопируйте `config/env.template` в `.env`, заполните `TELEGRAM_BOT_TOKEN` (опционально `DATA_DIR`, `DATABASE_PATH`).
4. Запустите бота:
   ```bash
   python3 -m bot.main
   ```

## Использование
1. `/start` — приветствие и меню.
2. 🚀 Start <тест> — запуск теста (клавиатура скрывается на время прохождения).
3. 📊 <тест> results — последние результаты (для Hogan вместе с радаром).
4. 🔁 Restart <тест> — начать тест заново.
5. `/reset` — стереть свои ответы/результаты и вернуть стартовое меню.

## Тесты и утилиты
- Юнит-тесты: `pytest` (радар, клавиатура меню).
- Экспорт HEXACO (админ): `python scripts/export_hexaco.py --output exports`.

## Структура
- `bot/` — код бота (handlers, services, keyboards, utils).
- `docs/` — продуктовая/техническая документация, методики тестов.
- `data/` — локальная SQLite-база (создаётся автоматически).
